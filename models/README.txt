This directory stores trained model artefacts generated by src/train.py:

  fake_news_model.h5   — Keras LSTM model weights
  tokenizer.pkl        — fitted Keras Tokenizer

Run `python src/train.py` (or `bash run.sh`) to populate this directory.
